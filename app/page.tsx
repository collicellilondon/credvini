'use client';
import {useState} from 'react'; import {Logo} from '@/components/Logo'; import {Lock,Eye,EyeOff,ArrowRight,ShieldCheck} from 'lucide-react';
export default function Login(){const [show,setShow]=useState(false);const [loading,setLoading]=useState(false);const enter=(e:React.FormEvent)=>{e.preventDefault();setLoading(true);localStorage.setItem('cv_session','active');setTimeout(()=>location.href='/dashboard',350)};return <main className="login"><section className="loginBrand"><Logo/><div className="hero"><span className="eyebrow">GESTÃO FINANCEIRA LOCAL</span><h1>Seu dinheiro sob<br/><em>controle absoluto.</em></h1><p>Clientes, contratos e recebimentos organizados em uma experiência simples e profissional.</p><div className="security"><ShieldCheck/><span><b>Privacidade por padrão</b><small>Os registros ficam somente neste navegador</small></span></div></div><footer>© 2026 CredVini</footer></section><section className="loginPanel"><form onSubmit={enter}><div className="mobileLogo"><Logo/></div><span className="eyebrow">BEM-VINDO DE VOLTA</span><h2>Acesse sua conta</h2><p>Entre com suas credenciais para continuar.</p><label>E-mail<input type="email" defaultValue="vinicius@credvini.com" required/></label><label>Senha<div className="password"><input type={show?'text':'password'} defaultValue="CredVini@2026" required/><button aria-label={show?'Ocultar senha':'Mostrar senha'} type="button" onClick={()=>setShow(!show)}>{show?<EyeOff/>:<Eye/>}</button></div></label><button className="primary" disabled={loading}>{loading?'Entrando...':'Entrar no sistema'}<ArrowRight/></button><div className="safe"><Lock/> Acesso local e exclusivo</div><small className="demo">Credenciais de acesso já preenchidas</small></form></section></main>}
